{% extends "templates/layout.jinja2" %}
{% block content %}

    {% if context.is_empty %}
        <p><em>No children.</em></p>
    {% else %}
        <ul>
            {% for child in context.values() %}
                <li>
                    <a href="{{ request.resource_url(child) }}">{{ child.title }}</a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
    <form action="{{ request.resource_url(context, 'add_folder') }}"
          method="POST">
        <label for="folder_title">Add: Folder
            Title</label>
        <input name="folder_title"/>
        <input type="submit"/></form>
    <form action="{{ request.resource_url(context, 'add_document') }}"
          method="POST">
        <label for="document_title">Add: Document
            Title</label>
        <input name="document_title"/>
        <input type="submit"/></form>

{% endblock content %}
